# Minimal broker config for the MQTT integration test: listen on all interfaces (so a container's
# mapped port is reachable) and allow anonymous connections (the test uses no credentials). Demo/
# test only - never run a real broker with anonymous access enabled.
listener 1883
allow_anonymous true
